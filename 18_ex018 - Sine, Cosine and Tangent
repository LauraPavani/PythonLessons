from math import cos, sin, tan, radians

an = float(input('Insert the angle that you want: '))
sen = sin(radians(an))
co = cos(radians(an))
ta = tan(radians(an))

print('The sine of {} is {:.2f} \nThe tangent of {} is {:.2f} \nThe cosine of {} is {:.2f}.'.format(an, sen, an, ta, an, co))
